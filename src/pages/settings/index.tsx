// Imports
import utils from "../../utils/";

function settings() {
  utils.title("settings");

  return (
    <>
      {utils.translate.use("pages.settings.title")}
    </>
  );
};

export { settings };
